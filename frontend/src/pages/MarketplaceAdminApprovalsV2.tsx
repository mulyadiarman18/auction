import { useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { ArrowLeft, Check, Clock3, Download, FileCheck2, History, X } from "lucide-react";
import { Link } from "react-router-dom";
import { toast } from "sonner";
import { AuctionHeader } from "@/components/auction/MarketHeader";
import { AdminRoleBar } from "@/components/admin/AdminRoleBar";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { apiGet, apiPatch } from "@/lib/api";
import type { AdminAuditLog, AdminRole, Bidder, BuyerProfile, BuyerVerificationUpdate } from "@/types/auction";

interface MarketplaceAdminApprovalsV2Props { bidder: Bidder; onBidderChange: (bidder: Bidder) => void; adminRole: AdminRole; onAdminRoleChange: (role: AdminRole) => void }
const adminNames: Record<AdminRole, string> = { super_admin: "Dina Super Admin", reviewer: "Raka Reviewer", finance: "Fina Finance MPI", inspector: "Indra Inspektor" };

export default function MarketplaceAdminApprovalsV2({ bidder, onBidderChange, adminRole, onAdminRoleChange }: MarketplaceAdminApprovalsV2Props) {
  const client = useQueryClient(); const [reasons, setReasons] = useState<Record<string, string>>({}); const roleQuery = `admin_role=${adminRole}`;
  const buyersQuery = useQuery({ queryKey: ["admin-buyers", "PENDING", adminRole], queryFn: () => apiGet<BuyerProfile[]>(`/admin/buyers?status=PENDING&${roleQuery}`), retry: false });
  const auditQuery = useQuery({ queryKey: ["admin-audit-logs", adminRole], queryFn: () => apiGet<AdminAuditLog[]>(`/admin/audit-logs?${roleQuery}`), retry: false });
  const mutation = useMutation({ mutationFn: ({ buyerId, status }: { buyerId: string; status: BuyerVerificationUpdate["verification_status"] }) => apiPatch<BuyerProfile>(`/admin/buyers/${buyerId}?${roleQuery}`, { verification_status: status, admin_name: adminNames[adminRole], reason: reasons[buyerId] ?? "" }), onSuccess: async (buyer) => { await Promise.all([client.invalidateQueries({ queryKey: ["admin-buyers"] }), client.invalidateQueries({ queryKey: ["admin-summary"] }), client.invalidateQueries({ queryKey: ["admin-audit-logs"] })]); toast.success(buyer.verification_status === "APPROVED" ? "Peserta disetujui" : "Peserta ditolak", { description: `${buyer.full_name} diperbarui oleh ${adminNames[adminRole]}.` }); } });
  const buyers = buyersQuery.data ?? []; const audits = auditQuery.data ?? [];
  return <div data-testid="admin-approvals-page" className="min-h-screen bg-[#f6f7f9] text-slate-800"><AuctionHeader bidder={bidder} onBidderChange={onBidderChange} /><main className="mx-auto max-w-[1000px] px-4 py-7 sm:px-6 lg:px-8"><AdminRoleBar role={adminRole} onRoleChange={onAdminRoleChange} /><Link data-testid="admin-approvals-back-link" to="/admin" className="mb-5 inline-flex items-center gap-2 text-xs font-semibold text-slate-500 hover:text-[#03AC0E]"><ArrowLeft size={14} /> Kembali ke Admin Console</Link><div data-testid="admin-approvals-heading-block" className="rounded-xl bg-slate-800 p-6 text-white sm:p-8"><p data-testid="admin-approvals-eyebrow" className="text-xs font-semibold text-[#8DE68F]">Verification queue · {adminNames[adminRole]}</p><h1 data-testid="admin-approvals-heading" className="mt-2 font-heading text-3xl font-bold">Approval bidder</h1><p data-testid="admin-approvals-description" className="mt-2 max-w-xl text-sm text-slate-300">Hanya peserta yang lolos auto screening dan memiliki dokumen lengkap yang tampil di antrean ini.</p></div><section data-testid="admin-approvals-list-section" className="mt-5 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div><p data-testid="admin-approvals-list-eyebrow" className="text-xs font-semibold text-[#03AC0E]">Ready for review</p><h2 data-testid="admin-approvals-list-heading" className="mt-1 font-heading text-xl font-bold">Peserta lolos screening</h2></div><Badge data-testid="admin-approvals-count" variant="outline" className="border-amber-200 bg-amber-50 text-amber-700"><Clock3 size={13} className="mr-1" /> {buyers.length} pending</Badge></div>{!buyersQuery.isError && buyers.length === 0 && <div data-testid="admin-approvals-empty" className="p-12 text-center"><FileCheck2 data-testid="admin-approvals-empty-icon" size={32} className="mx-auto text-[#03AC0E]" /><p data-testid="admin-approvals-empty-title" className="mt-4 font-semibold">Tidak ada peserta pending.</p></div>}{buyers.length > 0 && <div data-testid="admin-approvals-list" className="divide-y divide-slate-100">{buyers.map((buyer) => <div data-testid={`admin-approval-row-${buyer.id}`} key={buyer.id} className="p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p data-testid={`admin-approval-name-${buyer.id}`} className="text-sm font-semibold">{buyer.full_name}</p><Badge data-testid={`admin-approval-screening-${buyer.id}`} className="bg-[#E8F9EA] text-[10px] text-[#03AC0E] hover:bg-[#E8F9EA]">Screening {buyer.screening_status}</Badge></div><div className="mt-2 grid gap-1 text-xs text-slate-500 sm:grid-cols-2"><p data-testid={`admin-approval-email-${buyer.id}`}>{buyer.email}</p><p data-testid={`admin-approval-phone-${buyer.id}`}>{buyer.phone}</p><p data-testid={`admin-approval-identity-${buyer.id}`}>{buyer.identity_number}</p><p data-testid={`admin-approval-date-${buyer.id}`}>{new Date(buyer.created_at).toLocaleDateString("id-ID", { dateStyle: "medium" })}</p></div><div data-testid={`admin-approval-documents-${buyer.id}`} className="mt-3 flex flex-wrap gap-2">{buyer.documents.map((document) => <a data-testid={`admin-document-download-${document.id}`} key={document.id} href={`/api/admin/buyers/${buyer.id}/documents/${document.id}?${roleQuery}`} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1.5 rounded-lg bg-slate-100 px-2.5 py-1.5 text-[11px] font-semibold text-slate-600 hover:bg-[#E8F9EA] hover:text-[#03AC0E]"><Download size={12} /> {document.file_name}</a>)}</div></div><div className="flex shrink-0 gap-2"><Button data-testid={`admin-approve-button-${buyer.id}`} type="button" disabled={mutation.isPending} onClick={() => mutation.mutate({ buyerId: buyer.id, status: "APPROVED" })} className="bg-[#03AC0E] text-white hover:bg-[#02930c]"><Check size={15} /> Setujui</Button><Button data-testid={`admin-reject-button-${buyer.id}`} type="button" disabled={mutation.isPending || (reasons[buyer.id]?.trim().length ?? 0) < 5} variant="outline" onClick={() => mutation.mutate({ buyerId: buyer.id, status: "REJECTED" })} className="border-red-200 text-red-600 hover:bg-red-50"><X size={15} /> Tolak</Button></div></div><label data-testid={`admin-reject-reason-label-${buyer.id}`} htmlFor={`reason-${buyer.id}`} className="mt-4 block text-[11px] font-semibold text-slate-500">Alasan review (wajib untuk penolakan)</label><input id={`reason-${buyer.id}`} data-testid={`admin-reject-reason-${buyer.id}`} value={reasons[buyer.id] ?? ""} onChange={(event) => setReasons((current) => ({ ...current, [buyer.id]: event.target.value }))} placeholder="Contoh: nomor identitas tidak sesuai dokumen" className="mt-1.5 h-9 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 text-xs outline-none focus:border-[#03AC0E]" /></div>)}</div>}</section><section data-testid="admin-audit-section" className="mt-5 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center gap-3 border-b border-slate-100 p-5"><History size={19} className="text-[#03AC0E]" /><div><p data-testid="admin-audit-eyebrow" className="text-xs font-semibold text-[#03AC0E]">Immutable activity</p><h2 data-testid="admin-audit-heading" className="font-heading text-xl font-bold">Audit keputusan</h2></div></div><div data-testid="admin-audit-list" className="divide-y divide-slate-100">{audits.length === 0 && <p data-testid="admin-audit-empty" className="p-6 text-sm text-slate-500">Belum ada keputusan tercatat.</p>}{audits.map((audit) => <div data-testid={`admin-audit-row-${audit.id}`} key={audit.id} className="flex flex-col gap-2 p-5 sm:flex-row sm:items-center sm:justify-between"><div><p data-testid={`admin-audit-entity-${audit.id}`} className="text-sm font-semibold">{audit.entity_name}</p><p data-testid={`admin-audit-reason-${audit.id}`} className="mt-1 text-xs text-slate-500">{audit.reason}</p></div><div className="text-left sm:text-right"><Badge data-testid={`admin-audit-action-${audit.id}`} className={audit.action === "APPROVE" ? "bg-[#E8F9EA] text-[#03AC0E]" : "bg-red-50 text-red-600"}>{audit.old_status} → {audit.new_status}</Badge><p data-testid={`admin-audit-admin-${audit.id}`} className="mt-1 text-[10px] text-slate-400">{audit.admin_name} · {new Date(audit.created_at).toLocaleString("id-ID")}</p></div></div>)}</div></section></main></div>;
}