import { useQuery } from "@tanstack/react-query";
import { ArrowLeft, Check, FileCheck2, Gauge, MapPin, ShieldCheck } from "lucide-react";
import { Link, useParams } from "react-router-dom";
import { AuctionHeader } from "@/components/auction/MarketHeader";
import { MiningBidPanel as MarketBidPanel } from "@/components/auction/MiningBidPanel";
import { Badge } from "@/components/ui/badge";
import { apiGet } from "@/lib/api";
import type { Bid, Bidder, Lot } from "@/types/auction";

interface Props { bidder: Bidder; onBidderChange: (bidder: Bidder) => void }
const money = (value: number) => `Rp ${value.toLocaleString("id-ID")}`;

export default function MiningLotDetail({ bidder, onBidderChange }: Props) {
  const { lotId } = useParams(); const lotQuery = useQuery({ queryKey: ["lot", lotId], queryFn: () => apiGet<Lot>(`/lots/${lotId}`), enabled: Boolean(lotId), retry: false }); const bidsQuery = useQuery({ queryKey: ["lot-bids", lotId], queryFn: () => apiGet<Bid[]>(`/lots/${lotId}/bids`), enabled: Boolean(lotId), retry: false }); const lot = lotQuery.data;
  return <div data-testid="lot-detail-page" className="min-h-screen bg-[#f4f4f2] text-[#202124]"><AuctionHeader bidder={bidder} onBidderChange={onBidderChange} /><main className="mx-auto max-w-[1240px] px-4 py-5 sm:px-6 lg:px-8"><Link data-testid="lot-detail-back-link" to="/#catalog" className="mb-5 inline-flex items-center gap-2 text-xs font-semibold text-slate-500 hover:text-[#9A6700]"><ArrowLeft size={14} /> Kembali ke katalog unit</Link>{lotQuery.isLoading && <div data-testid="lot-detail-loading" className="rounded-xl bg-white p-12 text-center text-sm text-slate-500">Memuat data unit...</div>}{lot && <><p data-testid="lot-detail-breadcrumb" className="mb-4 text-xs text-slate-400">Katalog unit <span className="px-2">›</span> {lot.category} <span className="px-2">›</span> {lot.unit_number}</p><div className="grid gap-5 lg:grid-cols-[1.4fr_0.8fr]"><div className="space-y-5"><section data-testid="lot-detail-gallery" className="overflow-hidden rounded-xl border border-slate-200 bg-white"><div className="relative aspect-[16/9]"><img data-testid="lot-detail-main-image" src={lot.image_url} alt={lot.title} className="h-full w-full object-cover" /><Badge data-testid="lot-detail-category" className="absolute left-4 top-4 bg-[#F5B800] text-[#202124]">{lot.category}</Badge><div data-testid="lot-detail-inspection-badge" className="absolute bottom-4 left-4 rounded-lg bg-white px-3 py-2 shadow-lg"><p className="text-[10px] text-slate-400">Skor inspeksi</p><p className="font-mono font-bold text-[#9A6700]">{lot.inspection_score}<span className="text-xs font-normal text-slate-400"> / 100</span></p></div></div><div className="p-5"><div className="flex flex-wrap items-center gap-2"><span data-testid="lot-detail-number" className="font-mono text-xs font-bold text-[#9A6700]">{lot.lot_number}</span><span data-testid="lot-detail-unit-code" className="rounded bg-slate-100 px-2 py-1 font-mono text-[10px] text-slate-600">{lot.registration_code}</span>{lot.seller_verified && <span data-testid="lot-detail-seller-verified" className="flex items-center gap-1 text-xs text-slate-700"><ShieldCheck size={14} className="text-[#F5B800]" /> Vendor terverifikasi</span>}</div><h1 data-testid="lot-detail-title" className="mt-3 font-heading text-2xl font-bold sm:text-3xl">{lot.title}</h1><p data-testid="lot-detail-location" className="mt-3 flex items-center gap-1.5 text-sm text-slate-500"><MapPin size={15} className="text-[#B77900]" /> {lot.location}</p></div></section><section data-testid="lot-specs-section" className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-center justify-between"><div><p data-testid="lot-specs-eyebrow" className="text-xs font-bold text-[#9A6700]">Technical sheet</p><h2 data-testid="lot-specs-heading" className="mt-1 font-heading text-xl font-bold">Identitas dan kondisi unit</h2></div><Gauge size={22} className="text-[#B77900]" /></div><div data-testid="lot-specs-grid" className="mt-5 grid grid-cols-2 gap-4 sm:grid-cols-4">{[["No. Unit", lot.unit_number], ["No. Lambung", lot.hull_number], ["Merk / Tipe", `${lot.brand} ${lot.model_type}`], ["Tahun", lot.year], ["Jam Operasi", lot.operating_hours], ["Transmisi", lot.transmission], ["Grade", lot.grade], ["Serial Number", lot.vin], ["Harga pembuka", money(lot.base_price)], ["Increment", money(lot.minimum_increment)], ["Vendor", lot.seller_name], ["Lokasi Pool", lot.pool_location]].map(([label, value], index) => <div data-testid={`lot-spec-${index}`} key={String(label)} className="border-t border-slate-100 pt-3"><p data-testid={`lot-spec-label-${index}`} className="text-[10px] text-slate-400">{label}</p><p data-testid={`lot-spec-value-${index}`} className="mt-1 text-sm font-semibold leading-relaxed text-slate-700">{value}</p></div>)}</div></section><section data-testid="lot-description-section" className="rounded-xl border border-slate-200 bg-white p-5"><p className="text-xs font-bold text-[#9A6700]">Catatan inspeksi</p><h2 className="mt-1 font-heading text-xl font-bold">Kondisi dan attachment</h2><p data-testid="lot-description-text" className="mt-3 text-sm leading-relaxed text-slate-500">{lot.description}</p><div data-testid="lot-feature-list" className="mt-4 grid gap-2 sm:grid-cols-2">{lot.features.map((feature, index) => <p data-testid={`lot-feature-${index}`} key={feature} className="flex items-center gap-2 text-xs text-slate-600"><Check size={14} className="text-[#B77900]" /> {feature}</p>)}</div></section></div><aside className="lg:sticky lg:top-28 lg:h-fit"><div data-testid="lot-detail-current-bid" className="mb-3 rounded-xl border border-slate-200 bg-white p-5"><p className="text-xs text-slate-500">Bid tertinggi saat ini</p><p className="mt-1 font-mono text-2xl font-bold text-[#9A6700]">{money(lot.current_bid)}</p><p className="mt-2 text-[11px] text-slate-400">Harga limit hanya dapat dilihat Admin MPI dan vendor.</p></div><MarketBidPanel lot={lot} bids={bidsQuery.data ?? []} bidder={bidder} /></aside></div><section data-testid="lot-detail-bottom-bar" className="mt-5 flex items-center gap-3 rounded-xl border border-slate-200 bg-white p-4"><FileCheck2 size={20} className="text-[#B77900]" /><div><p className="text-sm font-semibold">Kelengkapan dokumen unit</p><p data-testid="lot-document-description" className="mt-1 text-xs text-slate-500">{lot.document_completeness.join(" · ")}</p></div><span data-testid="lot-document-status" className="ml-auto hidden text-xs font-semibold text-[#8A5B00] sm:block">{lot.document_status}</span></section></>}</main></div>;
}